# Vimshottari Dasha CLI 実装記録

## もともとのplan

- 仕様と既存差分、入力JSON例を確認する。
- Dasha計算・期間境界・デフォルト期間を完成させる。
- Legacy検証、batch CLI、JSON出力を完成させる。
- 単体テストとREADMEを追加・更新する。
- ビルド、テスト、受入条件を確認する。

## planから変更した点

初期実装は、入力リクエストJSONと参照チャートJSONの区別、Legacy検証、深さ3〜5の既定期間を十分に実装できていなかった。そのため、DashaCommand/DashaCalculatorを仕様ベースで作り直し、`source.chartFile` 解決、期間探索、LegacyChartValidator、バッチのError継続処理を追加した。

## 最終結果

- Vimshottariのlord順と365.25日計算を実装した。
- Nakshatra、Pada、lord、fractionCompletedを再計算するようにした。
- depth 1〜5、明示period、referenceDateTime、深さ別既定期間、入れ子のoverlap filteringを実装した。
- 固定UTC offset、ミリ秒切り捨て、JSONのnull/空children省略を実装した。
- Legacy必須フィールド検証とWarning生成を実装した。
- 複数入力、glob、重複排除、古い出力削除、バッチ集計を実装した。
- READMEとDashaCommandTestsを追加した。
- `dotnet build AstroCli.slnx` 成功、`dotnet test AstroCli.slnx --no-build --maxcpucount:1` で29件成功を確認した。
- VSTestはサンドボックス内ではソケット作成権限により失敗するため、権限昇格した実行環境で確認した。

## TODOの状態

すべて完了。既存の `deliverables` 配下の削除変更は今回の作業と無関係なため保持した。
