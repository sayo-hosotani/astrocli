# 2026-06-27 01:35 Note

## Topic

天体位置とASCの計算エンジンを、Astronomy EngineからSharpAstrology.SwissEph Moshierへ切り替える方針を整理する。

## User Request

SharpAstrology.SwissEphを調査した結果、MoshierならC#ライブラリ単体で動き、C由来DLLや天文暦ファイルが不要であることが分かった。

今後は次の方針で進める。

- 現在のAstronomy Engine実装は `astronomy-engine` ブランチに退避済み。
- いまは `SharpAstrology.SwissEph` のMoshierへ切り替える。
- Moshierと高精度モードの差は気になる。
- Moshierで精度が不十分なら、`astronomy-engine` ブランチを参照して戻す可能性を残す。
- Swiss/JPL高精度ファイルは現時点では使わない。

## Original Plan

### 1. Scope Definition

理由/特記事項:

ハウス追加と計算エンジン差し替えを同時に行うと、差分の原因が追いにくくなる。まず天体位置とASCをSharpAstrology.SwissEph Moshierへ切り替え、既存出力形式を維持する。

- [x] 現在のAstronomy Engine実装が `astronomy-engine` ブランチに退避済みであることを記録する。
- [x] Swiss/JPL高精度ファイルは使わない方針を記録する。
- [x] Moshierで精度が不十分ならAstronomy Engineへ戻す可能性を記録する。
- [x] 次goalはハウス追加ではなく、計算エンジン切り替えにする。

### 2. Goal Draft

理由/特記事項:

ユーザー確認前に、Codexが作業を進めるために必要な目的、要件、達成条件だけを `goal.md` にまとめる。

- [x] 現在の `goal.md` を履歴として `docs/goals/20260627-0135-goal.md` に保存する。
- [x] `goal.md` をSharpAstrology.SwissEph Moshier切り替えのDraftに更新する。
- [x] ユーザーに `goal.md` のレビューを依頼する。
- [x] ユーザー承認後、Codex goalに設定する。

## Plan Changes

なし。

## Final Result

議事録作成時点では、方針整理と `goal.md` Draft作成まで完了。

- 次goalは、天体位置とASCの計算エンジンをSharpAstrology.SwissEph Moshierへ切り替えること。
- CLI入力形式とJSON出力形式は維持する。
- ハウス追加はこの次のgoalに分ける。
- goal設定済み。

## Implementation Plan

### 1. Current State And API Confirmation

理由/特記事項:

SharpAstrology.SwissEphへの切り替えでは、既存の出力構造を維持しつつ、計算部分だけを置き換える。まず現在の依存、計算コード、テストを確認する。

- [x] 現在のcsproj依存を確認する。
- [x] 現在の天体計算処理を確認する。
- [x] 現在のASC計算処理を確認する。
- [x] 現在のテスト構造を確認する。

### 2. Dependency Switch

理由/特記事項:

今回のgoalの中心は計算エンジンの切り替え。`SharpAstrology.SwissEph` を追加し、不要になった `CosineKitty.AstronomyEngine` は削除する。

- [x] `SharpAstrology.SwissEph` を追加する。
- [x] `SharpAstrology.Base` が推移依存または明示依存として解決されることを確認する。
- [x] `CosineKitty.AstronomyEngine` 依存を削除する。
- [x] restore/buildで依存関係が解決されることを確認する。

### 3. Calculator Rewrite

理由/特記事項:

JSON出力形式は維持し、内部計算だけをMoshierへ切り替える。ASCはSharpAstrology.SwissEphのハウス計算由来のCross Ascを使う。

- [x] SharpAstrology.SwissEph Moshierのcontext生成を追加する。
- [x] 主要10天体をSharpAstrologyの `Planets` enumにマッピングする。
- [x] 天体黄経を `PlanetsPosition` から取得する。
- [x] ASCを `HouseCuspPositions(..., HouseSystems.Placidus)` の `Cross[Asc]` から取得する。
- [x] 既存の `BodyPosition` 出力形式へ変換する。

### 4. Tests

理由/特記事項:

計算エンジンを変えるため、固定値はMoshierの値へ更新する。比較テストはAstronomy EngineではなくSharpAstrology.SwissEph由来であることを確認する形に変える。

- [x] 固定日時・固定位置のスナップショット値をMoshier値に更新する。
- [x] 主要10天体の黄経比較テストをSharpAstrology.SwissEph基準に更新する。
- [x] ASC比較テストをSharpAstrology.SwissEphのCross Asc基準に更新する。
- [x] 既存の入力/位置情報テストが維持されることを確認する。

### 5. Documentation And Verification

理由/特記事項:

READMEと検証結果を最新化し、通常のbuild/test/CLI実行でgoal達成を確認する。

- [x] READMEに計算エンジンがSharpAstrology.SwissEph Moshierであることを記載する。
- [x] `dotnet build AstroCli.slnx` を実行する。
- [x] `dotnet test AstroCli.slnx --no-build` を実行する。
- [x] CLI実行結果を確認する。
- [x] 議事録にplan/do/doneを反映する。

## Implementation Plan Changes

なし。

## Implementation Result

完了。

最終状態:

- `SharpAstrology.SwissEph` 0.4.2を追加した。
- `CosineKitty.AstronomyEngine` 依存を削除した。
- `SharpAstrology.Base` 0.12.0と `System.Numerics.Tensors` 10.0.7は推移依存として解決された。
- 天体位置は `SwissEphemeridesService(ephType: EphType.Moshier)` の `PlanetsPosition` から取得するようにした。
- ASCは `HouseCuspPositions(..., HouseSystems.Placidus)` の `Cross[Asc]` から取得するようにした。
- Swiss Ephemeris `.se1` ファイル、JPL `.eph` ファイル、C由来ネイティブDLLは使用していない。
- CLI入力形式、JSON出力構造、主要10天体、60進数表記は維持した。
- READMEと `.agents/skills/test.md` をSharpAstrology.SwissEph Moshier前提に更新した。

確認事項:

- Moshier/Placidus由来の `Cross[Asc]` は、固定条件で `114°29’59″`、Cancer、`24°29’59″` になった。
- 従来の自前ASC計算で出ていた `294°29’53″` は、SharpAstrology上では `Cross[Dc]` 側に近い値だった。
- そのため、スナップショットはSharpAstrology.SwissEph Moshierの `Cross[Asc]` に合わせて更新した。

検証:

- `dotnet build AstroCli.slnx` 成功。0 warnings, 0 errors。
- `dotnet test AstroCli.slnx --no-build` 成功。14 tests passed。
- `src/AstroCli/bin/Debug/net10.0/astrocli "1989-07-08 05:19:00 +09:00" "35°41’22″N,139°41’30″E"` 成功。
