# 2026-06-29 13:49 Note

## Topic

小惑星位置計算に光時間補正を追加する。

## User Request

小惑星だけ算出方法が違うため、小惑星だけ光時間補正を入れる。

## Original Plan

### 1. API Confirmation

理由/特記事項:

Astronomy Engineに光時間補正用APIがあるなら、独自反復より既存APIを使う方が意図と精度要件を追いやすい。

- [x] Astronomy Engineに光速定数または光時間補正APIがあるか確認する。
- [x] 現在の小惑星計算が幾何学的な同時刻地心ベクトルであることを確認する。

### 2. Implementation

理由/特記事項:

主要天体・ノード・ASC・ハウスは既存のSharpAstrology.SwissEph計算を維持し、小惑星の地心ベクトル計算だけを apparent geocentric に近づける。

- [x] 小惑星ごとの位置関数を追加する。
- [x] `Astronomy.CorrectLightTravel` で観測時刻から光出発時刻を解く。
- [x] 光出発時刻の小惑星位置と観測時刻の地球位置から地心ベクトルを作る。
- [x] 既存と同じEQJからECTへの変換、黄経算出、60進数整形を維持する。

### 3. Tests And Verification

理由/特記事項:

Fake Horizons clientの既存テストは、光時間補正で小惑星位置が指定黄経から少し動くため、補正後の挙動を直接検証する形に変える。

- [x] 小惑星位置計算が `CorrectLightTravel` を使った結果を返すことをテストする。
- [x] 通常チャート出力に小惑星が含まれるテストを維持する。
- [x] `dotnet build AstroCli.slnx` を実行する。
- [x] `dotnet test AstroCli.slnx --no-build` を実行する。
- [x] 可能なら固定日時・固定位置で通常CLIを実行し、小惑星出力の変化を確認する。

## Plan Changes

なし。

## Final Result

完了。

- 小惑星の地心ベクトル計算に `Astronomy.CorrectLightTravel` を追加した。
- 光出発時刻の小惑星位置と観測時刻の地球位置から、光時間補正済みの見かけ地心ベクトルを作るようにした。
- EQJからECTへの変換、黄経算出、60進数整形は既存処理を維持した。
- Fake Horizons clientを使う小惑星スナップショットを光時間補正後の値に更新した。
- `dotnet build AstroCli.slnx` 成功。0 warnings, 0 errors。
- `dotnet test AstroCli.slnx --no-build` 成功。22 tests passed。
- `src/AstroCli/bin/Debug/net10.0/astrocli "1989-07-08 05:19:00 +09:00" "36°24’00″N,139°20’00″E"` 成功。
- ユーザー確認により、光時間補正後の小惑星は比較サイトとの差が10-30秒程度に落ち着いた。
- 現時点では、この差を許容範囲として扱う。

固定条件での小惑星出力:

- `chiron`: `99°30’20″` Cancer `9°30’20″`
- `ceres`: `65°56’05″` Gemini `5°56’05″`
- `pallas`: `14°02’11″` Aries `14°02’11″`
- `juno`: `163°11’55″` Virgo `13°11’55″`
- `vesta`: `271°47’54″` Capricorn `1°47’54″`
