# 2026-06-27 01:55 Note

## Topic

SharpAstrology.SwissEph Moshierを使って、プラシーダスの12ハウスカスプをJSON出力に追加する。

## User Request

このままハウス対応に進む。ハウスシステムはプラシーダスにする。

直前の確認:

- SharpAstrology.SwissEph Moshier切り替え後、astro.comとの差分は水星が1秒違いのみ。
- 現時点では許容範囲と判断する。

## Original Plan

### 1. Scope Definition

理由/特記事項:

計算エンジンはすでにSharpAstrology.SwissEph Moshierへ切り替わっている。今回はハウスシステム追加に集中し、入力形式、主要10天体、ASCは維持する。

- [x] ハウスシステムをプラシーダスに確定する。
- [x] astro.comとの差分確認結果を記録する。
- [x] 現在の `goal.md` を履歴として `docs/goals/20260627-0155-goal.md` に保存する。
- [x] `goal.md` をプラシーダス12ハウス追加のgoalに更新する。
- [x] Codex goalに設定する。

### 2. Output Shape

理由/特記事項:

ハウスカスプも黄経・星座・サイン内度数で表現できる。既存の `BodyPosition` と同じ形にして、JSON上の読み方を揃える。

- [x] JSON出力に `houses` を追加する。
- [x] `houses.system` は `placidus` とする。
- [x] `houses.cusps.house1` から `houses.cusps.house12` を出力する。
- [x] 各house cuspには `name`、`eclipticLongitude`、`sign`、`degreeInSign` を含める。

### 3. Calculation

理由/特記事項:

SharpAstrology.SwissEphの `HouseCuspPositions(..., HouseSystems.Placidus)` はすでにASCでも使用しているため、同じ計算結果から12ハウスカスプを出す。

- [x] `HouseCuspPositions(..., HouseSystems.Placidus)` の `HouseCusps` を使用する。
- [x] `house1` は既存の `ascendant` と同じ黄経になることを確認する。
- [x] 度数表記は既存の60進数文字列を使う。

### 4. Tests And Verification

理由/特記事項:

新しいJSON構造を固定日時・固定位置で検証し、既存出力が壊れていないことを確認する。

- [x] 固定日時・固定位置のスナップショットテストに `houses` を追加する。
- [x] `house1` と `ascendant` の黄経一致をテストする。
- [x] `dotnet build AstroCli.slnx` を実行する。
- [x] `dotnet test AstroCli.slnx --no-build` を実行する。
- [x] CLI実行結果を確認する。

## Plan Changes

なし。

## Final Result

完了。

最終状態:

- JSON出力に `houses` を追加した。
- `houses.system` は `placidus`。
- `houses.cusps.house1` から `houses.cusps.house12` を出力する。
- 各house cuspは `name`、`eclipticLongitude`、`sign`、`degreeInSign` を持つ。
- `HouseCuspPositions(..., HouseSystems.Placidus)` の `HouseCusps` を使用する。
- `house1.eclipticLongitude` は `ascendant.eclipticLongitude` と一致する。
- READMEにプラシーダスハウス出力を追記した。

固定条件での主要な出力:

- `houses.system`: `placidus`
- `house1`: `114°29’59″` Cancer `24°29’59″`
- `house10`: `11°06’51″` Aries `11°06’51″`

検証:

- `dotnet build AstroCli.slnx` 成功。0 warnings, 0 errors。
- `dotnet test AstroCli.slnx --no-build` 成功。15 tests passed。
- `src/AstroCli/bin/Debug/net10.0/astrocli "1989-07-08 05:19:00 +09:00" "35°41’22″N,139°41’30″E"` 成功。
