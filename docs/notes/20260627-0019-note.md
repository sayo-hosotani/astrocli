# 2026-06-27 00:19 Note

## Topic

AstroCLIの次のgoal候補として、位置情報入力とASC（上昇点）出力の仕様を検討した。

## User Request

次のgoalについて会話したい。

- 占星術チャートでは位置情報を使用するはずなので、位置情報をオプション指定できるようにしたい。
- ASC（上昇点）の出力を追加したい。
- 位置情報がASCを出すのに使われるか確認したい。
- 今回は西洋占星術ネイタル固定のまま、簡単な指定方法で進めたい。

## Original Plan

### 1. Required Inputs

理由/特記事項:

占い種類が増えても、多くのチャート計算では日時と観測地点が基本入力になる。まず必須入力の考え方を整理する。

- [x] ASCに位置情報が必要か確認する。
- [x] 必須入力を日時と緯度経度として整理する。
- [x] 現在の日時指定形式を維持する。

### 2. CLI Input Shape

理由/特記事項:

最終的には全パラメータに `--parameter-name` 形式や短縮オプション、JSON入力を用意する予定だが、今は実装と検証を進めやすい簡単な指定にする。

- [x] 第1引数を日時にする。
- [x] 第2引数を位置情報にする。
- [x] 位置情報を1引数の文字列として扱う。
- [x] 位置情報の暫定形式を決める。

### 3. Latitude And Longitude Direction

理由/特記事項:

単純な数値だけの `"35.6895,139.6917"` では、南緯・西経の表現が分かりにくく、入力ミスにつながりやすい。人間が読みやすい占星術CLIとして方位付き表記を採用する。

- [x] 数値のみの緯度経度指定の問題を確認する。
- [x] 緯度には `N` または `S` を付ける仕様にする。
- [x] 経度には `E` または `W` を付ける仕様にする。
- [x] 内部では `N/E` を正、`S/W` を負のdecimal degreeに正規化する方針にする。

### 4. Next Goal Readiness

理由/特記事項:

goalに指定する前に、実装に必要な入力、出力、スコープが揃っているか確認する。

- [x] 今回は西洋占星術ネイタル固定のままとする。
- [x] 既存のSun/Moon出力は維持する。
- [x] JSON出力に `location` を追加する方針にする。
- [x] JSON出力に `ascendant` を追加する方針にする。
- [x] 度数表記は既存方針どおり60進数文字列にする。
- [x] `goal.md` を次goalとして更新する。
- [ ] goalに設定する。未実施: ユーザーの明示的な許可待ち。

## Plan Changes

### Replan 1

変更したこと:

- 位置情報の入力形式を `"latitude,longitude"` から `"latitudeN/S,longitudeE/W"` 形式へ変更した。

変更理由:

- `"35.6895,139.6917"` では北緯・東経以外、特に南緯・西経の表現が曖昧になるため。
- 符号付きdecimal degreeでも表現可能だが、占星術CLIとしては方位付きのほうが読みやすく入力ミスを減らせるため。

変更後のplan:

- 第2引数は1つの位置情報文字列にする。
- 緯度は `35.6895N` / `33.8688S` のように指定する。
- 経度は `139.6917E` / `74.0060W` のように指定する。
- CLI例は `astrocli "1989-07-08 05:19:00 +09:00" "35.6895N,139.6917E"` とする。

結果:

- 方位付き緯度経度形式で進めることで合意した。

## Decisions

次goal候補:

```text
西洋占星術ネイタル固定のまま、CLI入力に位置情報を追加し、JSON出力に location と ascendant を追加する。
```

入力例:

```bash
astrocli "1989-07-08 05:19:00 +09:00" "35.6895N,139.6917E"
```

入力仕様:

- 第1引数: 日時。形式は既存どおり `"yyyy-MM-dd HH:mm:ss zzz"`。
- 第2引数: 位置情報。形式は `"latitude,longitude"`。
- 緯度は末尾に `N` または `S` を付ける。
- 経度は末尾に `E` または `W` を付ける。
- 内部値は `N/E` を正、`S/W` を負のdecimal degreeとして扱う。

出力仕様:

- 既存のSun/Moon出力は維持する。
- JSONに `location` を追加する。
- JSONに `ascendant` を追加する。
- ASCの度数表記は既存方針どおり60進数文字列にする。

将来の方針:

- 全パラメータに `--parameter-name` 形式を用意する。
- 短縮パラメータを検討する。
- 引数で指定できるものはJSONでも指定できるようにする。
- JSONで指定できるものは引数でも指定できるようにする。

## Final Result

議事録作成時点では、次goalの仕様会話まで完了。

- ASCには日時と観測地点が必要であると整理した。
- 次goalに必要な入力仕様、出力仕様、スコープは揃っている。
- `goal.md` 更新は完了した。
- goal設定はまだ行っていない。

## Implementation Plan

### 1. Goal Activation And Current State

理由/特記事項:

ユーザー承認後の作業なので、`goal.md` の状態をActiveにし、既存実装の変更点を明確にしてから進める。

- [x] `goal.md` のCurrent StatusをActiveに更新する。
- [x] 既存のCLI引数パースを確認する。
- [x] 既存のChartRequest/ChartOutputを確認する。
- [x] 既存のNatalChartCalculatorを確認する。
- [x] 既存テストを確認する。

### 2. Location Input

理由/特記事項:

今回の新しい必須入力は位置情報であり、`N/S` と `E/W` の正負変換が仕様の中心になる。

- [x] 位置情報を表す型を追加する。
- [x] `"35.6895N,139.6917E"` 形式をパースする。
- [x] `N/E` を正、`S/W` を負に正規化する。
- [x] 不正な位置情報でusage errorを返す。
- [x] usage文を2引数形式に更新する。

### 3. Chart Output

理由/特記事項:

既存のSun/Moon出力を壊さず、JSONに `location` と `ascendant` を追加する。

- [x] `ChartRequest` に位置情報を追加する。
- [x] `ChartOutput` に `location` を追加する。
- [x] `ChartOutput` に `ascendant` を追加する。
- [x] `location.latitude` と `location.longitude` をdecimal degreeで出力する。

### 4. Ascendant Calculation

理由/特記事項:

Astronomy EngineにはASC専用APIは見当たらないが、Greenwich Apparent Sidereal Timeを返す `Astronomy.SiderealTime` と地理経度を使って地方恒星時を得られる。ASCは地方恒星時、緯度、黄道傾斜角から計算する。

- [x] Astronomy Engineの恒星時APIを使って地方恒星時を計算する。
- [x] ASC黄経を計算する。
- [x] ASCの星座名とサイン内度数を算出する。
- [x] 度数は既存の60進数フォーマッタで出力する。

### 5. Tests And Verification

理由/特記事項:

入力仕様と出力仕様が変わるため、既存テストを更新し、位置情報の正負変換とASC出力を固定条件で検証する。

- [x] 固定日時・固定位置のJSONスナップショットテストを更新する。
- [x] Astronomy Engine比較テストを新しいChartRequestに合わせて更新する。
- [x] `N/E` と `S/W` の正負変換テストを追加する。
- [x] 不正な位置情報のエラーテストを追加する。
- [x] `dotnet build AstroCli.slnx` を実行する。
- [x] `dotnet test AstroCli.slnx --no-build` を実行する。
- [x] CLI実行結果を確認する。

## Implementation Result

完了。

最終状態:

- CLI入力は `astrocli "1989-07-08 05:19:00 +09:00" "35.6895N,139.6917E"` 形式になった。
- 位置情報は `GeoLocation` としてパースし、`N/E` を正、`S/W` を負のdecimal degreeに正規化する。
- JSON出力に `location.latitude` と `location.longitude` を追加した。
- JSON出力に `ascendant` を追加した。
- 固定日時・固定位置では、ASCは `294°29’53″`、`Capricorn`、`24°29’53″` として出力される。
- 既存のSun/Moon出力は維持した。
- READMEと `.agents/skills` のbuild/test/実行説明を現在の通常実行に合わせて更新した。

検証:

- `dotnet build AstroCli.slnx` 成功。0 warnings, 0 errors。
- `dotnet test AstroCli.slnx --no-build` 成功。11 tests passed。
- `src/AstroCli/bin/Debug/net10.0/astrocli "1989-07-08 05:19:00 +09:00" "35.6895N,139.6917E"` 成功。
