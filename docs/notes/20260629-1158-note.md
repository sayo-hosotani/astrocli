# Note

## Topic

小惑星ツールで、毎回JPL Horizons用IDを調べなくてよいように、固定対象名からIDへ内部変換する。

## Decisions

- 対象小惑星は、キロン、セレス、パラス、ジュノー、ベスタに固定する。
- ID対応はコード内の固定マップで扱う。
- 今後対象を増やす場合は、その都度コード修正で対応する。
- 入力名は出力JSONの `id` に残し、変換後IDは `horizonsCommand` に出す。

## Original Plan

### 1. 仕様更新

- [x] 対象小惑星を固定する。
- [x] 入力で扱う日本語名と英名を決める。
- [x] 既存ID入力を互換として残す。

### 2. 実装

- [x] 名称からIDへのマップを専用クラスに分離する。
- [x] `AsteroidInputParser` でテキスト入力を正規化する。
- [x] `AsteroidInputParser` でJSON入力を正規化する。

### 3. 検証と記録

- [x] 日本語名入力のテストを追加する。
- [x] 英名入力のテストを追加する。
- [x] JSON名称入力のテストを追加する。
- [x] READMEを更新する。
- [x] `goal.md` とgoal履歴を更新する。
- [x] `dotnet build AstroCli.slnx` を通す。
- [x] `dotnet test AstroCli.slnx --no-build` を通す。

## Changes From Plan

- 実通信確認を追加で行い、`キロン` が `2060;` に変換されてJPL Horizonsから取得できることを確認した。

## Outcome

小惑星名からHorizons commandへの固定マップを追加し、テキスト入力、JSON入力、既存ID入力をすべて維持した。

検証:

- `dotnet build AstroCli.slnx` 成功。
- `dotnet test AstroCli.slnx --no-build` 成功。24件成功。
- `printf 'キロン\n' | src/AstroCli/bin/Debug/net10.0/astrocli asteroid --at "2026-06-29 22:00:00 +09:00" --location "35°41’22″N,139°41’30″E"` を実通信で確認し、`horizonsCommand` が `2060;` になることを確認した。
