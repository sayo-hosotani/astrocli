# 2026-06-27 00:45 Note

## Topic

緯度経度をdecimal degree表記から60進法表記へ変更する。

## User Request

占いサイトでは緯度経度に60進法を採用していることが多いため、AstroCLIの緯度経度を60進法に変更したい。

## Original Plan

### 1. Current State

理由/特記事項:

前goalで位置情報は `"35.6895N,139.6917E"` のdecimal degree入力、JSON出力は `location.latitude` / `location.longitude` の数値として実装済み。今回は内部計算ではdecimal degreeを維持し、外部表現を60進法へ変更する。

- [x] 既存の位置情報パーサを確認する。
- [x] 既存の位置情報JSON出力を確認する。
- [x] 既存テストを確認する。

### 2. Location Input

理由/特記事項:

CLIで人間が指定する緯度経度を、占いサイトでよく見る60進法の方位付き表記へ変更する。

- [x] 入力例を `"35°41’22″N,139°41’30″E"` に変更する。
- [x] 緯度を `度°分’秒″N/S` としてパースする。
- [x] 経度を `度°分’秒″E/W` としてパースする。
- [x] `N/E` を正、`S/W` を負のdecimal degreeへ正規化する。
- [x] 不正な60進法位置情報でusage errorを返す。

### 3. Location Output

理由/特記事項:

ユーザーの「緯度経度を60進法に変更」は、入力だけでなくJSON出力の `location` も含むと解釈する。内部計算値はdecimal degreeのまま保持する。

- [x] `location.latitude` を60進法文字列で出力する。
- [x] `location.longitude` を60進法文字列で出力する。
- [x] ASC計算は正規化後のdecimal degreeを使い続ける。

### 4. Documentation And Verification

理由/特記事項:

入力形式とJSON出力形式が変わるため、README、テスト、CLI確認を更新する。

- [x] READMEの実行例と説明を60進法位置情報に更新する。
- [x] スナップショットテストを更新する。
- [x] `N/E` と `S/W` の正負変換テストを60進法入力に更新する。
- [x] 不正入力テストを60進法前提に更新する。
- [x] `dotnet build AstroCli.slnx` を実行する。
- [x] `dotnet test AstroCli.slnx --no-build` を実行する。
- [x] CLI実行結果を確認する。

## Plan Changes

なし。

## Final Result

完了。

最終状態:

- CLIの位置情報入力は `"35°41’22″N,139°41’30″E"` のような60進法になった。
- `GeoLocation` は60進法をパースし、内部ではdecimal degreeに正規化する。
- JSON出力の `location.latitude` と `location.longitude` は60進法文字列になった。
- ASC計算は正規化後のdecimal degreeを引き続き使用する。
- 太陽・月などの天体位置はtopocentricではなく地心基準で計算することをREADMEに明記した。
- 位置情報はASCや将来のハウス計算など、観測地点の地平線・子午線が関係する要素に使う。
- README、`goal.md`、`.agents/skills/test.md` の位置情報例を60進法に更新した。

検証:

- `dotnet build AstroCli.slnx` 成功。0 warnings, 0 errors。
- `dotnet test AstroCli.slnx --no-build` 成功。13 tests passed。
- `src/AstroCli/bin/Debug/net10.0/astrocli "1989-07-08 05:19:00 +09:00" "35°41’22″N,139°41’30″E"` 成功。
