# 2026-06-29 13:24 Note

## Topic

テストで使う固定位置情報を変更する。

## User Request

テスト時の位置情報を `"36°24’00″N,139°20’00″E"` に変更する。

## Original Plan

### 1. Scope Confirmation

理由/特記事項:

依頼は「テストの時の位置情報」の変更なので、CLIの使用例や過去の議事録・goal履歴は原則として変更しない。固定スナップショットで位置に依存するASCとハウスカスプは期待値更新が必要になる。

- [x] 既存テストで固定位置を使っている箇所を確認する。
- [x] 位置入力パース用の汎用テストケースは対象外と判断する。

### 2. Test Update

理由/特記事項:

固定位置を差し替え、位置依存の期待値を新しい計算結果に合わせる。主要天体、ノード、小惑星Fake値は位置非依存なので維持する。

- [x] `VerificationLocation` を `"36°24’00″N,139°20’00″E"` に変更する。
- [x] 固定位置の `GeoLocation` 数値を `36.4, 139.33333333333334` に変更する。
- [x] JSON出力スナップショットの `location`、`ascendant`、`houses` 期待値を更新する。
- [x] 主要天体、ノード、小惑星Fake値が変わらないことを確認する。

### 3. Verification

理由/特記事項:

スナップショット更新なので、ビルドとテストで回帰を確認する。

- [x] `dotnet build AstroCli.slnx` を実行する。
- [x] `dotnet test AstroCli.slnx --no-build` を実行する。

## Plan Changes

なし。

## Final Result

完了。

- テスト固定位置を `"36°24’00″N,139°20’00″E"` に変更した。
- 固定位置の10進数値を `36.4, 139.33333333333334` に変更した。
- 新位置に合わせてASCとプラシーダスハウスカスプのスナップショットを更新した。
- 主要天体、ノード、小惑星Fake値は位置非依存のため維持した。
- `dotnet build AstroCli.slnx` 成功。0 warnings, 0 errors。
- `dotnet test AstroCli.slnx --no-build` 成功。22 tests passed。

新しい固定位置での位置依存スナップショット:

- `ascendant`: `114°34’02″` Cancer `24°34’02″`
- `house1`: `114°34’02″` Cancer `24°34’02″`
- `house10`: `10°43’33″` Aries `10°43’33″`
