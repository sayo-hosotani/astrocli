# 2026-06-27 01:08 Note

## Topic

AstroCLIの次goalとして、出力天体を主要10天体へ拡張する仕様を検討した。

## User Request

次のgoalでは、対応する天体を増やしたい。

対象:

- 太陽
- 月
- 水星
- 金星
- 火星
- 木星
- 土星
- 天王星
- 海王星
- 冥王星

## Discussion

- 冥王星は準惑星だが、今回の実装では他の惑星と同じ流れで扱う。
- Astronomy Engineの `Body.Pluto` を使える想定で、地心黄経を計算する。
- 天文計算ライブラリや暦表の違いにより、外惑星や冥王星では他サイトと秒単位の差が出る可能性はある。
- 今回は精度比較をgoalの中心にはせず、Astronomy Engineで主要10天体を出すことを目標にする。
- 既存方針どおり、太陽・月・惑星の位置はtopocentricではなく地心基準とする。
- `location` と `ascendant` は維持する。

## Original Plan

### 1. Scope Definition

理由/特記事項:

今回のgoalは、入力形式やASC計算を広げることではなく、既存の `bodies` 出力対象を主要10天体に拡張することに集中する。

- [x] 対象天体を主要10天体として確定する。
- [x] 入力形式は現状維持とする。
- [x] `location` と `ascendant` は現状維持とする。
- [x] 天体位置は地心基準とする。
- [x] 度数表記は既存の60進数文字列を維持する。

### 2. Goal Draft

理由/特記事項:

ユーザー確認前に、Codexが作業を進めるために必要な目的、要件、達成条件だけを `goal.md` にまとめる。

- [x] 現在の `goal.md` を履歴として `docs/goals/20260627-0108-goal.md` に保存する。
- [x] `goal.md` を主要10天体対応のDraftに更新する。
- [x] ユーザーに `goal.md` のレビューを依頼する。
- [x] ユーザー承認後、Codex goalに設定する。

## Plan Changes

なし。

## Final Result

議事録作成時点では、仕様整理と `goal.md` Draft作成まで完了。

- 出力対象を主要10天体へ拡張する方針にした。
- 冥王星は他の惑星と同じ計算フローで扱う。
- 秒単位の差が出る可能性は承知したうえで、今回はAstronomy Engineによる主要10天体出力をgoalとする。
- goal設定済み。

## Implementation Plan

### 1. Current Implementation

理由/特記事項:

既存の `bodies` はSun/Moon専用の型になっているため、まず構造を確認して10天体へ広げる最小変更点を決める。

- [x] `ChartOutput` の `BodiesOutput` 構造を確認する。
- [x] `NatalChartCalculator` の天体計算処理を確認する。
- [x] 既存テストのSun/Moon前提を確認する。

### 2. Bodies Expansion

理由/特記事項:

今回のgoalの中心は `bodies` に主要10天体を同じ形で追加すること。既存JSONの `bodies.sun` / `bodies.moon` の形を維持して追加する。

- [x] `BodiesOutput` に主要10天体のプロパティを追加する。
- [x] 主要10天体のBody定義を追加する。
- [x] `NatalChartCalculator` で主要10天体を計算する。
- [x] 既存の地心黄経計算と60進数表記を全天体で再利用する。

### 3. Tests

理由/特記事項:

出力対象が増えるため、固定日時・固定位置のスナップショットとAstronomy Engine比較の両方で10天体を検証する。

- [x] 固定日時・固定位置のJSONスナップショットテストを10天体に更新する。
- [x] Astronomy Engine比較テストを主要10天体に拡張する。
- [x] 既存のlocation/ascendant/入力エラーテストが維持されることを確認する。

### 4. Documentation And Verification

理由/特記事項:

ユーザー向け説明と実行確認を最新仕様に揃える。

- [x] READMEのMVP説明を主要10天体に更新する。
- [x] `dotnet build AstroCli.slnx` を実行する。
- [x] `dotnet test AstroCli.slnx --no-build` を実行する。
- [x] CLI実行結果を確認する。

## Implementation Plan Changes

なし。

## Implementation Result

完了。

最終状態:

- `bodies` に `sun`、`moon`、`mercury`、`venus`、`mars`、`jupiter`、`saturn`、`uranus`、`neptune`、`pluto` を出力するようにした。
- 各天体は `name`、`eclipticLongitude`、`sign`、`degreeInSign` を持つ。
- 既存の `location`、`ascendant`、入力形式は維持した。
- 天体位置は地心基準で、既存の `Astronomy.GeoVector` と60進数フォーマッタを再利用する。
- READMEのMVP説明を主要10天体対応に更新した。

検証:

- `dotnet build AstroCli.slnx` 成功。0 warnings, 0 errors。
- `dotnet test AstroCli.slnx --no-build` 成功。13 tests passed。
- `src/AstroCli/bin/Debug/net10.0/astrocli "1989-07-08 05:19:00 +09:00" "35°41’22″N,139°41’30″E"` 成功。

## External Site Comparison

ユーザー確認結果:

- astro.comと比較すると、外惑星である天王星、海王星、冥王星に数秒程度の差が出る。
- 現時点では許容し、実装はこのまま維持する。
- 差の原因としては、利用している天文計算ライブラリ、暦表、丸め、時刻系、計算モデルの違いが考えられる。
