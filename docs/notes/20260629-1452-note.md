# 20260629-1452 Note

## Subject

通常チャートの出力JSONについて、既存の `bodies` と `ascendant` を見直し、用途別のトップレベルセクションに分割した。

## Original Plan

### 現行JSON生成箇所とテスト期待値を調査する

理由: 出力契約の変更なので、モデル、計算、テスト、READMEの依存箇所を先に確認する必要があった。

- [x] `ChartOutput` と `NatalChartCalculator` の現行構造を確認した。
- [x] `AppTests` のJSON期待値とモデル参照を確認した。
- [x] READMEの出力例を確認した。

### bodies分割、angles追加、ascendant削除、nodes objects化を実装する

理由: ユーザー要望の中心であり、JSON出力の互換性が変わる。

- [x] `bodies` を `planets`、`asteroids`、`angles`、`objects` に分割した。
- [x] `angles` に `asc`、`ic`、`dsc`、`mc` を出力するようにした。
- [x] 既存トップレベル `ascendant` を削除した。
- [x] ノードを `objects` に移した。

### テストとREADMEの期待JSONを更新する

理由: 出力契約の変更をテストで固定し、利用者向けの説明も一致させる必要があった。

- [x] JSONテストを新しいセクション構造に更新した。
- [x] 旧 `ascendant` と旧 `bodies` が出力されないことをテストした。
- [x] READMEのMVP説明と出力例を更新した。

### ビルドとテストで確認する

理由: record構造とテスト参照を変更したため、コンパイルと既存挙動の回帰確認が必要だった。

- [x] `dotnet build AstroCli.slnx` が成功した。
- [x] `dotnet test AstroCli.slnx --no-build` が成功した。

### 議事録にplan/do/doneを記録する

理由: `.agents/rules/workflow.md` の作業ルールに従うため。

- [x] この議事録を作成した。

## Plan Changes

なし。

## Decisions

- `planets`、`asteroids`、`angles`、`objects` はトップレベルキーとして出力する。
- `angles` のJSONキーは `asc`、`ic`、`dsc`、`mc` とする。
- `objects` には `northNode` と `southNode` を出力する。
- 既存の `houses` は今回の変更対象外として維持する。

## Result

通常チャートJSONは、主要10天体を `planets`、小惑星を `asteroids`、ASC/IC/DSC/MCを `angles`、ノードを `objects` に分けて出力する形になった。トップレベルの `ascendant` と旧 `bodies` は出力されない。

検証:

```text
dotnet build AstroCli.slnx
dotnet test AstroCli.slnx --no-build
```

どちらも成功した。
